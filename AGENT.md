# Agent context — Norwegian Honey

> **Moved to Cursor rules.** Persistent AI instructions live in [`.cursor/rules/`](.cursor/rules/):
>
> | Rule | Scope |
> |------|--------|
> | `project.mdc` | Role, architecture, ethics, code principles (always on) |
> | `operations.mdc` | Makefile, investigation workflow, pitfalls (always on) |
> | `python.mdc` | FastAPI/service patterns (`app/**`, `scripts/**`) |
>
> User docs: `README.md`, `deploy/DEPLOY.md`, `make.env.example`.
